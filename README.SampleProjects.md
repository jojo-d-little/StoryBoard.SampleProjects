# StoryBoard.SampleProjects

Content-only NuGet package containing StoryBoard sample projects for testing.

Install it with:

```xml
<PackageReference Include="StoryBoard.SampleProjects" Version="1.0.0" />
```

The package preserves the `Samples/` folder and copies its contents to the consuming project's output directory.
