# StoryBoard.SourceAssets

Content-only NuGet package containing source images and sounds used by StoryBoard sample projects for testing.

Install it with:

```xml
<PackageReference Include="StoryBoard.SourceAssets" Version="1.0.0" />
```

The package preserves the `SourceAssets/` folder and copies its contents to the consuming project's output directory.
